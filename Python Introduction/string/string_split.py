# split() Function

# ! split() is the opposite of join(). It turns a string with a certain separator into a list.

str="Imagination will take you everywhere"

res=str.split()

print(res)

# ? Output : ['Imagination', 'will', 'take', 'you', 'everywhere']
